package com.flowkode.mailvalidator

import com.xenomachina.argparser.ArgParser

internal class MailValidatorArgs(parser: ArgParser) {
//    val workDir: Path by parser.storing(
//            "-d", "--data-directory",
//            help = "The data directory, state and users are stored on it, defaults to the working directory.") {
//        Paths.get(this)
//    }.default(Paths.get("."))
}
